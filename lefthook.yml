pre-commit:
  commands:
    check:
      run: qlty check -a
      stage_fixed: true
    smells:
      run: qlty smells -a
      stage_fixed: true
commit-msg:
  commands:
    lint:
      run: pnpm exec commitlint -e
